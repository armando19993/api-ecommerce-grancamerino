meta {
  name: Update User
  type: http
  seq: 4
}

put {
  url: {{base_url}}/users/{{user_id}}
  body: json
  auth: bearer
}

auth:bearer {
  token: {{auth_token}}
}

body:json {
  {
    "name": "John Doe Updated",
    "email": "john.updated@example.com",
    "is_admin": true
  }
}
